# 本次交付验证记录

日期：2026-09-23。本目录为旧20260921方案的修订交付，源仓库和旧方案未修改。

## 已完成

- 审阅最新stream顶层、参数/类型、controller、DMA、scratchpad、Delayer、SA/CMP、RawFMA/FP32RawFMA、Accumulator、banked SRAM、构建脚本及测试入口；对照8FSA与旧方案。
- 阅读仓库AGENTS、PROJECT_CONTEXT及maintaining-project-context skill；源码与新综合对比报告优先，过时上下文信息在本目录审计中纠正。
- 阅读TAPA-CS arXiv v1相关章节，将布局、互连流水化、HBM绑定对应到可控实验。具体split-d架构为本项目建议。
- 运行新版 `reference_split_d.py`，输出 `reference_results.json`。

| 自检 | 实际范围/结果 |
|---|---|
| 标准数学配置 | 9组L/D/R/BQ/BK × 2种causal × 4种E，共72例PASS |
| 错误注入 | 8种均被检出：scale_by_rows、clear_each_d、double_seed、omit_alpha、rowsum_per_v_tile、overwrite_p、rescale_entire_o_each_v、normalize_only_rows |
| score/P生命周期 | seed回写、每d/每key计数，P跨V保持，MAX/ROWSUM/PV ledger通过 |
| mask/history/alpha | 全mask列、首tile无历史、后续首次有效、后续空tile、max增长通过 |
| packing/layout | 12组外存FP16/FP32打包，4组Q/K/V双buffer片上布局通过 |
| causal流量 | 3组非方形/尾波参数×causal/noncausal共6例：枚举集合与公式一致 |
| 广播 | 80packet/4consumer的pending-mask抽象检查，无丢重；不是RTL流控仿真 |
| FTZ | 半精度最小正规数阈值、阈值以下清零、有符号零样例通过；不是RawFMA位模型 |
| 流量示例 | 历史770、当前515、共享67、三区195 MiB公式检查通过；不是性能实测 |

`validate_delivery.py` 检查UTF-8、Markdown围栏/本地链接、源文件和旧文档hash快照，以及上述输出的通过状态。实际文件数量、Python版本、误差数值、交付文件hash见 [delivery_validation.json](delivery_validation.json)；源输入见 [source_manifest.json](source_manifest.json)。

## 明确未执行

本次没有实现新HLS代码，没有运行仓库C++回归、HLS Csim/综合、RTL cosim、IP导出、Vivado实现或上板。当前仓库副本缺`third_party/vitis_hls/include/ap_int.h`，没有原始build目录可核对。综合对比文档的7.300ns和32DSP是历史报告记录，不是本次结果。

Python使用FP64普通乘加/真实exp，模拟单PE复用reg及score种子语义；没有FP32逐FMA舍入、完整S16/N16/X16/PWL、SRAM端口、有限FIFO调度、AXI时序或物理放置。选用P16/FTZ时也只有P量化样例，不能据其误差给硬件精度背书。E循环是功能顺序模拟，不证明硬件并行。

## 复现

在本目录执行：

```powershell
python reference_split_d.py
python validate_delivery.py
```

生成物仍写入本目录；validate脚本遇到源/旧方案hash变化会失败，要求重新审计，不静默刷新输入快照。语法/文件检查和模型通过不能替代03要求的实际HLS实施验收。
