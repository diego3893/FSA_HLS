# fsa_stream II=1 远端迭代修改日志

## 1. 本次调用信息

- 开始时间：2026-09-03 02:20 +08:00
- 当前状态：进行中
- 本地仓库：`C:\Users\30130\Desktop\workstation\FlashAttention\FSA_HLS`
- 远端仓库：`FSA-FPGA-NM37-tailBox:~/FSA_HLS`（绝对路径待远端预检确认）
- 分支：`perf/fsa-streaming-core-v1`
- 起始commit：`dfb694052b632f90492d9a1464efc29fe44cb25a`
- 目标模块：`fsa_stream_request`（用户称 `fsa_core`，当前综合主路径的流式 FSA 核）
- 工具链：Vitis HLS 2024.2（初始化方式待远端探测）
- 最大迭代次数：2

## 2. 期望结果与验收标准

### 用户期望

通过 SSH 在 NM37 远端验证当前流式 FSA 核，目标为 II=1，并确认综合结构包含 4x4 PE、4 个 CMP、Input/Output Delayer 和 4 路 Accumulator；最多完成两轮“修改、推送、远端测试、报告分析”闭环。

### 可验证标准

- [ ] 对应顶层 C 仿真通过，完整 9x4 Q/K/V 测试数值正确。
- [ ] C 综合成功，目标器件、100 MHz 时钟和 4x4 阵列配置保持不变。
- [ ] PE 的核心 token 处理循环综合 II=1；顶层若因 DATAFLOW 无固定事务间隔而不报告单一 II，需以子模块循环报告与协同仿真吞吐证据说明。
- [ ] 综合层次中存在 16 个 PE、4 个 CMP、Input/Output Delayer 和 4 路独立 Accumulator。
- [ ] RTL 协同仿真完成，无死锁，并记录实际延迟/间隔报告。
- [ ] 检查估算时钟、DSP/LUT/FF/BRAM/URAM 和关键 warning，无新增阻断性问题。

## 3. 初始状态

- 本地工作树：分支与 `origin/perf/fsa-streaming-core-v1` 同步；无暂存或已跟踪未提交改动。存在两组本轮不处理的未跟踪文件：`docs/fsa_stream_request综合报告.md`、`skills/fsa-hls-remote-iteration/`。
- 远端预检：未执行。
- 相关源码和既有测试：起始 HEAD 已包含 `stream_array` 的 16 路显式 PE 任务、4 路显式 Accumulator 调用、动态 wave tripcount，以及 `fsa_stream_request` 的 CSim/CSynth/CoSim 流程开关。
- 初始问题证据：本地旧构建 `build/fsa_stream_request_build/solution1` 对应起始 HEAD 之前的源码，只能作为问题基线；其综合成功但顶层 DATAFLOW 延迟/II 为 `?`，结构为 16 PE、4 CMP、Delayer，Accumulator 当时只综合出一条复用 lane，且未执行 CoSim。起始 HEAD `dfb6940` 已针对 scalar FIFO/动态参数警告和 4 路 Accumulator 做修改，尚需远端重综合验证。

## 4. 迭代总览

| 轮次 | 被测commit | 修改摘要 | 本地测试 | 远端测试 | 验收状态 |
|---:|---|---|---|---|---|
| 1 | 待定 | 验证起始 HEAD 中的 DATAFLOW 与 4 路 Accumulator 修改 | 待执行 | 待执行 | 进行中 |

## 5. 逐轮记录

### 第1轮

#### 修改前判断与计划

- 当前问题：旧综合报告的顶层延迟/II 不可知，且 Accumulator 只形成一条 lane；起始 HEAD 的修复尚无对应远端 HLS 证据。
- 证据：`build/fsa_stream_request_build/solution1/syn/report/` 及旧 `solution1.log`；起始 HEAD 为 `dfb6940`。
- 原因假设：DATAFLOW 区域中的动态/标量依赖使工具无法形成规范任务网络；循环形式的 lane 调用被调度为硬件复用而非 4 路空间实例。
- 本轮计划：先跑本地回归；仅提交本日志（源码修复已在起始 HEAD）；推送并在远端精确检出 `dfb6940` 对应代码，执行 4x4 `fsa_stream_request` 的 CSim、CSynth 和 CoSim，再从真实层次、循环和资源报告判断是否达到要求。

#### 实际修改

- `docs/修改日志/2026-09-03_0220_fsa_stream_ii1_修改日志.md`：建立本次调用的可追溯记录。
- 源码修改：本轮开始时已包含在 `dfb6940`，远端验证前不额外修改。
- 与计划的偏差：无。

#### 修改后本地测试

| 命令 | 结果/退出码 | 关键证据 |
|---|---|---|
| `.\run_test.ps1 fsa_stream_request_top` | PASS，0 | reset + 9 requests，完整 9x4 Q/K/V；max L error=0.00654554，max O error=0.00217265；stream 最大深度 8。 |
| `.\run_test.ps1 fsa_stream_vs_legacy` | PASS，0 | tile boundary 与 legacy 匹配；stream 最大深度 8。 |
| `.\run_test.ps1 accumulator_top` | PASS，0 | RESET/invalid/SET_SCALE/ACC/ACC_SA/EXP_S1/EXP_S2/RECIPROCAL 全部通过。 |
| `.\run_test.ps1 fsa_dma_top` | PASS，0 | non-causal 和 causal 的完整 9x4 O 通过；stream 最大深度 36。 |

#### 修改后远端测试

- 被测commit：待定
- 环境与参数：待补充
- 命令：待补充
- 开始/结束时间：待补充
- 结果与退出码：待补充
- 关键指标：待补充
- 证据路径：待补充

#### 本轮结论与下一步

- 已解决的问题：待远端数据分析。
- 仍存在的问题：待远端数据分析。
- 验收标准状态：进行中。
- 失败分析：待远端数据分析。
- 下一轮修改：待第1轮远端数据决定。
- 本轮闭环状态：进行中。

## 6. 调用结束总结

- 结束时间：待补充
- 结束原因：进行中
- 已完成闭环迭代：0/2
- 未完成迭代：第1轮进行中
- 最终被测代码commit：待定
- 最终日志commit：未提交（进行中）
- 验收结果：待补充
- 仍未解决：待补充
- 建议下一步：待补充
- 独立最终报告：未要求
