# Vitis HLS 本地头文件说明

## 用途

本目录存放 Vitis HLS 提供的 C++ 头文件，用于：

- 让本地 VS Code 识别 ap_int、ap_fixed 和 hls::stream 等类型；

- 提供代码补全、定义跳转和基础语法检查；

- 在条件允许时使用普通 C++ 编译器进行部分功能测试。

这些头文件主要用于本地代码编辑。最终的 C 仿真、C/RTL 协同仿真和硬件综合仍应在安装了 Vitis HLS 的服务器上完成。

## 统一版本

本项目使用：

**Vitis HLS 2024.2**